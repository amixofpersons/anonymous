<div class='posts'>
<% @posts.each do |post| %>
  <h2>
    <%= post.score %> |
    <%= link_to image_tag("caret8.png", :border => 0), votes_path(post_id: post.id), method: :post%>
    <%= link_to post.title, post_path(post) %>
  </h2>
  <p><%= post.body %></p>
<% end %>
</div>